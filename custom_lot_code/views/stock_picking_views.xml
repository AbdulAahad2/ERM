<odoo>
    <record id="view_stock_move_operations_inherit" model="ir.ui.view">
        <field name="name">stock.move.operations.inherit</field>
        <field name="model">stock.move</field>
        <field name="inherit_id" ref="stock.view_stock_move_operations"/>
        <field name="arch" type="xml">
            <xpath expr="//widget[@name='generate_serials']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//widget[@name='import_lots']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//widget[@name='generate_serials']" position="after">
                <button name="action_open_auto_generate_lots"
                        type="object"
                        string="Generate Lots"
                        class="btn-link"
                        icon="fa-magic"
                        invisible="has_tracking == 'none'"/>
                <button name="generate_serials" invisible="1"/>
            </xpath>
        </field>
    </record>
</odoo>